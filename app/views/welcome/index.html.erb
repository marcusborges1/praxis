<section class="container">
  <div class="well col-md-4 col-md-offset-4">
    <% unless notice.nil? %>
      <p class="alert alert-info"><%= notice %></p>
    <% end %>
    <p class="text-center">Bem-vindo(a) ao sistema de Avaliação da Praxis!</p>
  </div>
</section>
