<!DOCTYPE html>
<html>
    <head>
        <title>Praxis</title>
        <%= csrf_meta_tags %>
        <%= stylesheet_link_tag    "application", :media => "all" %>
        <%= javascript_include_tag "application" %>
        <%= tag :meta, name: "viewport", content: "width=device-width, initial-scale=1.0" %>
    </head>

    <body>
        <%= render partial: 'layouts/menu' if user_signed_in? %>
        <%= yield %>
    </body>
</html>
